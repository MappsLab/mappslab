/* eslint-disable no-undef */
import React from 'react'

// import Mapp from '../index'

describe('Initial Rendering', () => {
	it('should show a warning when a marker has been supplied without lat or lang', async () => {
		//...assertion
	})

	it('should call event handlers attached to markers', async () => {
		//...assertion
	})
})
