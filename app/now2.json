{
	"version": 2,
	"alias": "mappslab-beta.now.sh",
	"name": "mappslab-beta",
	"builds": [{ "src": "public/*", "use": "@now/static" }],
	"routes": [{ "src": "/(.*)", "dest": "/public/$1" }, { "src": "/.*", "dest": "/public/index.html" }]
}
