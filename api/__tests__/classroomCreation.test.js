// @flow

import { request } from './utils/db'
import { getDBClassrooms } from './utils/classroom'

describe('[createClassroom]', () => {
	it.skip('should create a new classroom and assign the teacher', async () => {
		/* Arrange */
		// const { container, getByTestId } = render( ... )
		/* Act */
		/* Assert */
		// expect(...)
	})

	it.skip('should only allow teachers and admins to create new classrooms', async () => {
		/* Arrange */
		// const { container, getByTestId } = render( ... )
		/* Act */
		/* Assert */
		// expect(...)
	})

	it.skip('should only allow admins to assign other teachers to a new classroom', async () => {
		/* Arrange */
		// const { container, getByTestId } = render( ... )
		/* Act */
		/* Assert */
		// expect(...)
	})
})

describe('[assignStudent]', () => {
	it.skip('should only allow teachers to assign students to classrooms they teach in', async () => {
		/* Arrange */
		// const { container, getByTestId } = render( ... )
		/* Act */
		/* Assert */
		// expect(...)
	})
})
