// @flow

import { request } from './utils/db'
// import { removeUser } from '../UserModel'

describe('[createUser]', () => {
	it.skip('', () => {
		expect(2).toBe(2)
	})
	// afterAll(() => {
	//		removeUser(newUser)
	// })
	// it('should throw an error if no email was supplied', async () => {
	// })
	// it('should add a new teacher to the database', async () => {
	// })
	// it('should add a verification token & expiration to their data', async () => {
	// })
	// it('should send an email with their verification link', async () => {
	// })
	// it('should validate that user when the query is submitted', async () => {
	// })
})
